import Layout from "./Layout/Layout";
import Cards from "./Components/Cards";

const App = (props) => {
    return (
        <Layout>
            <Cards />
        </Layout>
    );
};

export default App;
